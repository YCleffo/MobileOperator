<Page x:Class="MobileOperator.View.Pages.ServicesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MobileOperator.View.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Услуги">

    <Grid>
        <DataGrid x:Name="RateDataGrid" ColumnWidth="*" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Название тарифа" Binding="{Binding RateName}"/>
                <DataGridTextColumn Header="Интернт" Binding="{Binding Internet}"/>
                <DataGridTextColumn Header="Количество минут" Binding="{Binding CountMinuts}"/>
                <DataGridTextColumn Header="Количество SMS" Binding="{Binding CountSms}"/>
            </DataGrid.Columns>
        </DataGrid>   
    </Grid>
</Page>
